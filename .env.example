# Supabase — Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Anthropic — console.anthropic.com → API Keys
ANTHROPIC_API_KEY=

# App URL (local dev uses port 3002)
NEXT_PUBLIC_APP_URL=http://localhost:3002

# Contact form — resend.com (verify your domain, then set FROM address)
RESEND_API_KEY=
CONTACT_FROM_EMAIL=Fineprint <admin@crosstalent.io>

# Stripe — dashboard.stripe.com (for /billing checkout)
STRIPE_SECRET_KEY=
STRIPE_PRICE_CREDITS=
STRIPE_PRICE_PRO=
